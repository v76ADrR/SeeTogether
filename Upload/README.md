# SeeTogether Upload

Local drop destination for the Upload Session dock.

Absolute path: `/home/leafyishere/Current_Projects/SeeTogether/Upload`

Dev server writes here via `POST /api/upload` (Vite plugin in `vite.config.ts`).
No cloud. No classification yet — files land flat in this folder.
